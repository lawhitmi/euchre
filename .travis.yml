language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  - "3.7"
  - "nightly" #nightly build
# command to install dependencies
install:
  - pip install -r requirements.txt
#command to run tests
script:
  #- ./gradlew build --scan -s
  - python ./src/euchre/euchre.py